{
  "name": "env-guardian",
  "version": "1.0.0",
  "description": "Validate all env vars before deploy. Never crash on startup from missing API keys.",
  "main": "dist/index.js",
  "bin": {
    "env-guardian": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "dotenv": "^16.3.1",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
